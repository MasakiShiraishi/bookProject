[build]
  base = "bookProject/BookFrontend"
  command = "npm install -g @angular/cli && npm install && npx ng build --configuration production"
  publish = "dist/book-frontend/browser"
  functions = "netlify/functions"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
